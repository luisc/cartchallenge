require 'spec_helper'

RSpec.describe Order do
  
  # it "sums the prices of its line items" do
  #   order = build(:order_with_line_items)
  #   expect(order.total).to eq(BigDecimal.new('15'))
  # end
  
  # it "applies promotion" do
  #   expect(true).to eq(false)
  # end
end